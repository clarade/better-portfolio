import ReactDOM from "react-dom";
import Router from "./Router";
import "./App.css";

ReactDOM.render(
    <React.StrictMode>
        <Router />
    </React.StrictMode>,
    document.getElementById("root")
);
